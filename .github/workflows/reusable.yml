name: Reusable Workflow

on:
  workflow_call:
    inputs:
      message:
        required: true
        type: string

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Message: ${{ inputs.message }}"
